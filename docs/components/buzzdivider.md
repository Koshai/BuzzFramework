# BuzzDivider

`BuzzDivider` provides visual separation between content blocks.

## Basic usage

```razor
<BuzzDivider Variant="default" />
```

## Parameters and effects

- `Variant`: `default`, `strong`, `subtle`.
