# BuzzBadge

`BuzzBadge` displays compact state labels for statuses and tags.

## Basic usage

```razor
<BuzzBadge Text="Resolved" Variant="success" />
```

## Parameters and effects

- `Text`: badge text.
- `Variant`: `neutral`, `info`, `success`, `warning`, `danger`.
